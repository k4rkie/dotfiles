require("modules.general-hardware") -- monitors, input, gestures, cursor env
require("modules.general-ui")       -- general/decoration/animations
require("modules.layer-rules")      -- layer-shell blur + animations
require("modules.window-rules")     -- window + workspace rules
require("modules.keybinds")         -- all keybinds + submaps
require("modules.autostart")        -- hyprland.start hook

